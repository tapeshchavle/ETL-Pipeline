name: 'foodingo'
version: '1.0.0'
config-version: 2

profile: 'foodingo'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  foodingo:
    staging:
      +schema: analytics
      +materialized: view
    facts:
      +schema: analytics
      +materialized: table
    marts:
      +schema: analytics
      +materialized: table
